{
  "name": "clawtique",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "bun run --filter './packages/core' build && bun run --filter './packages/*' build",
    "clean": "rm -rf packages/*/dist packages/*/*.tsbuildinfo",
    "test": "bun test packages/"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
